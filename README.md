# apt

Bootstrap files for `apt.libertai.io`.

Today `install.sh` installs the latest `libertai-cli` Debian package from the
GitHub Release assets. The signed APT repository metadata can replace this
bootstrap once GitHub Pages TLS, the packaging GPG key, and repository indexes
are fully wired.
